 #include<stdio.h>
int main()
{
    int t,a,b,c,i;
    scanf("%d",&t);
    for(i=0;i<t;i++)
    {
        scanf("%d %d %d",&a,&b,&c);
        if(a<=0 || b<=0 ||c<=0)
        {
            printf("Case %d: no",i+1);
        }
        else
        {
            if((c*c)==(a*a)+(b*b))
            {
                printf("Case %d: yes",i+1);
            }
            else
            {
                printf("Case %d: no",i+1);
            }

        }

    }
    return 0;
}

        else
        {
            if((c*c)==(a*a)+(b*b))
            {
                printf("Case %d: yes",i+1);
            }
            else
            {
                printf("Case %d: no",i+1);
            }

        }

    }
    return 0;
}

        else
        {
            if((c*c)==(a*a)+(b*b))
            {
                printf("Case %d: yes",i+1);
            }
            else
            {
                printf("Case %d: no",i+1);
            }

        }

    }
    return 0;
}
